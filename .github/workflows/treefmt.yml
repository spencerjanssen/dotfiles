name: Check formatting
on:
  pull_request:
  push:
    branches:
      - main
      - flake-update
jobs:
  check_formatting:
    name: Run treefmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      - uses: cachix/install-nix-action@56a7bb7b56d9a92d4fd1bc05758de7eea4a370a8 # v31.6.0
      - run: |
          nix run .#treefmt-wrapper
      - uses: reviewdog/action-suggester@5c1296fb4e4a63159e0033bfc2d6cf253ee6e872 # v1.23.0
        with:
          tool_name: treefmt
          level: warning
          fail_level: any
          filter_mode: nofilter
